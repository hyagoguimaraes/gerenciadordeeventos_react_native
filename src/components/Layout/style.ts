import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  layoutWrapper: {
    flex: 1,
    backgroundColor: "#fff",
  },
  
  mainContent: {
    flex: 1,
    paddingBottom: 70,
    backgroundColor: "#fff",
  },
});